# 一般向け版 開発工程表

作成日: 2026-05-19

対象アプリ: `apps/retirement-life-planner-public`

現行安定版: `apps/retirement-life-planner`

## 1. 工程全体の考え方

一般向け版は、現行の本人・夫婦向けアプリを直接改造しない。

作業は `apps/retirement-life-planner-public` で進め、現行版は比較・回帰確認用の安定版として残す。

開発の中心は機能追加ではなく、次の再設計である。

- 初回入力体験
- 一般向けの用語
- 税金・社会保険の要約表示
- 詳細入力と専門補正の分離
- サンプルデータと実データの分離
- 保存方式と免責
- 限定公開前の品質確認

## 2. 到達目標

### 2.1 最初の到達目標

限定公開できる一般向け版を作る。

この段階では、広い一般公開や課金よりも、次を優先する。

- 実データが混入しない
- 初回ウィザードだけで試算できる
- 税社保後に使えるお金が分かる
- 詳細入力を開かなくても判断できる
- 計算が概算であることが明示されている
- 現行版の主要計算結果を壊していない

### 2.2 公開判定の基準

限定公開前に、少なくとも次を満たす。

- `npm run test:public` が成功する
- `npm run build:public` が成功する
- PC幅、タブレット幅、スマートフォン幅で主要画面が破綻しない
- サンプル世帯で初回入力から結果確認まで迷わず完了できる
- 税金・社会保険タブが要約から始まる
- 免責と対応年度が画面上で確認できる
- 実データ、個人名、QNAP共有保存前提が一般向け版に残っていない

## 3. フェーズ構成

## Phase 0: 作業場所と管理体制の固定

目的: 現行版と一般向け版を混在させない。

状態: 着手済み

主な作業:

- `apps/retirement-life-planner-public` を新設する
- `docs/public-app` を新設する
- ルートの npm スクリプトに一般向け版を追加する
- 一般向け版のローカルURLを `http://127.0.0.1:5176/` に分ける
- 作業ブランチを `codex/general-public-app` に分ける

成果物:

- `apps/retirement-life-planner-public`
- `docs/public-app`
- `docs/public-app/workplan.md`

完了条件:

- `npm run test:public` が成功する
- `npm run build:public` が成功する
- 現行版 `apps/retirement-life-planner` に不要な変更が入っていない

## Phase 1: 現行版から一般向け版への棚卸し

目的: 何を残し、何を隠し、何を作り直すかを決める。

主な作業:

- 画面、入力項目、計算ロジック、保存処理を棚卸しする
- 個人利用前提の文言と導線を一覧化する
- QNAP共有保存、ローカル保存、サンプルデータの扱いを確認する
- 税社保計算で一般向けに残す項目を確定する
- 一般向け版で初期表示しない専門項目を確定する

成果物:

- `docs/public-app/current-app-inventory.md`
- `docs/public-app/migration-policy.md`

完了条件:

- 現行版から流用するコード領域が明確になっている
- 作り直すUI領域が明確になっている
- 一般向け版に残してはいけない個人前提が一覧化されている

## Phase 2: 情報設計と入力分類の確定

目的: 一般ユーザーが迷わず最初の試算まで進める構造を決める。

主な作業:

- 初回ウィザードのステップを確定する
- 通常タブ構成を確定する
- 入力項目を標準入力、詳細入力、専門補正に分類する
- 各入力項目の表示名、補助文、初期値、必須・任意を決める
- 専門用語のやさしい表示名と正式名称を整理する
- 初期サンプル世帯を定義する

成果物:

- `docs/public-app/information-architecture.md` の更新
- `docs/public-app/input-classification.md` の更新
- `docs/public-app/glossary.md`
- `docs/public-app/sample-households.md`

完了条件:

- 初回ウィザードの入力項目が確定している
- 初期表示しない専門項目が確定している
- 用語のやさしい表示名と正式名称が対応している

## Phase 3: データモデルと保存方針の整理

目的: 一般向け版で扱うデータの範囲と保存責任を明確にする。

主な作業:

- 現行の Zustand ストアと保存処理を確認する
- 一般向け版の初期データを匿名サンプルに置き換える
- LocalStorage 保存、インポート、エクスポートの扱いを決める
- QNAP共有保存APIへの依存を一般向け版から外すか、明確に無効化する
- 将来のアカウント保存に備えたデータ境界を整理する
- データ削除とリセット導線を設計する

成果物:

- `docs/public-app/data-storage-policy.md`
- 匿名サンプルデータ
- データリセット方針

完了条件:

- 一般向け版に実データが含まれていない
- 保存場所が画面上またはヘルプで説明できる
- QNAP家族内共有前提が一般向け版の通常導線に残っていない

## Phase 4: 初回ウィザード実装

目的: 最小入力だけで試算結果まで到達できるようにする。

主な作業:

- 初回ウィザードのルーティングまたは画面状態を作る
- 家族、資産、収入、支出、やりたいこと、結果の各ステップを実装する
- 入力の途中保存を設計する
- 入力不足時のメッセージを実装する
- 詳細入力へ進む導線を用意する
- 初回完了後に通常タブへ遷移する

成果物:

- 初回ウィザードUI
- 標準入力コンポーネント
- 初回結果サマリー

完了条件:

- 初回ウィザードだけで資産寿命と税社保後の使えるお金が表示される
- 詳細入力を開かずに初回試算が完了する
- スマートフォン幅で入力が破綻しない

## Phase 5: ダッシュボード再設計

目的: 一般ユーザーが最初に見るべき結論を前面に出す。

主な作業:

- ダッシュボードの主要カードを再構成する
- 資産寿命、90歳残高、指定年齢残高を整理する
- 税社保後の使えるお金を中心指標にする
- 健康寿命期の楽しみ支出余地を表示する
- 税社保が最も重い年、翌年負担に注意する年を表示する
- チャートの凡例と言葉を一般向けに変える

成果物:

- 一般向けダッシュボード
- 税社保要約カード
- 健康寿命期の資産活用サマリー

完了条件:

- 結果の意味がダッシュボードだけで概ね分かる
- 税社保が単なる支出として埋もれていない
- 専門表を見なくても注意年が分かる

## Phase 6: 税金・社会保険タブ再設計

目的: 税社保を隠さず、ただし専門表から始めない構成にする。

主な作業:

- 税社保タブの初期表示を要約にする
- 年ごとの税社保負担を表示する
- 負担が増える年と原因を自然文で表示する
- 所得発生年度と支払年度の違いを分かりやすく表示する
- 譲渡益税と翌年申告利益を分けて表示する
- 詳細表を折りたたみ式にする
- 対応年度と概算表示を入れる

成果物:

- 税社保要約ビュー
- 税社保詳細ビュー
- 税社保注意年の説明文

完了条件:

- 税社保タブが要約から始まる
- なぜ負担が増えたかが年単位で分かる
- 詳細表を開けば計算根拠を確認できる
- 概算であることが明示されている

## Phase 7: シナリオ比較再設計

目的: 情報量の多い比較結果を、判断に使える形へ整理する。

主な作業:

- 比較タブを結論、理由、詳細の順に再構成する
- 基準シナリオとの差をユーザー目線で表示する
- 良い差、悪い差、中立の差を整理する
- 税社保増分、生活後余力、資産寿命への影響を並べる
- 詳細表は後段に移す

成果物:

- シナリオ比較サマリー
- 差分理由ビュー
- 詳細比較表

完了条件:

- どのシナリオが何に効いたか分かる
- 税社保増分と手取り影響が混同されていない
- 赤字・青緑表示の意味が明確である

## Phase 8: タイムバケット一般化

目的: 本人専用のやりたいこと整理を、一般ユーザーが自然に使える形にする。

主な作業:

- やりたいことテンプレートを作る
- 旅行、趣味、家族イベント、学び、住まい、医療介護などに分類する
- 夫婦それぞれの希望を扱えるようにする
- 金額未定のイベントを仮置きできるようにする
- 特別支出化するかどうかの判断補助を作る
- 健康寿命期の資産活用額とつなげる

成果物:

- やりたいこと入力UI
- タイムバケット再設計版
- 特別支出化の補助導線

完了条件:

- 金額未定でも登録できる
- 健康寿命期に使う支出として整理できる
- 特別支出とやりたいことが混乱しない

## Phase 9: 免責、ヘルプ、用語、信頼性表示

目的: 税社保を中核にするアプリとして、説明責任を最低限満たす。

主な作業:

- 対応年度を表示する
- 概算であることを表示する
- 自治体差と制度改正の注意を表示する
- 専門家確認の案内を表示する
- 用語ヘルプを整備する
- プライバシーと保存先の説明を整備する

成果物:

- 免責表示
- 用語ヘルプ
- 保存先説明
- 対応年度表示

完了条件:

- ユーザーが計算の性格を誤解しにくい
- ただし免責が強すぎて使えない印象になっていない
- 実額が分かれば補正できる方針が示されている

## Phase 10: QA、回帰テスト、限定公開準備

目的: 限定公開できる品質まで確認する。

主な作業:

- 主要ケースのテストを追加する
- 現行版と一般向け版の計算差分を確認する
- サンプル世帯で通し操作を確認する
- レスポンシブ表示を確認する
- 実データ混入を確認する
- リリースチェックリストを更新する

成果物:

- 回帰テスト
- 手動QA記録
- 限定公開チェック結果

完了条件:

- `npm run test:public` が成功する
- `npm run build:public` が成功する
- 限定公開チェックリストが完了している
- 重大な既知不具合が残っていない

## 4. 優先順位

### 最優先

1. 実データを混ぜない
2. 現行版を壊さない
3. 初回ウィザードだけで試算できる
4. 税社保後の使えるお金を前面に出す
5. 概算・免責・対応年度を明示する

### 次点

1. シナリオ比較の見やすさ
2. タイムバケットの一般化
3. 詳細入力の整理
4. 用語ヘルプ
5. 印刷またはPDF向けレポート

### 後回し

1. ユーザー認証
2. 家族共有権限
3. クラウド保存
4. 課金
5. 自治体別の精密モデル

## 5. 依存関係

初回ウィザードは、入力分類が確定していないと実装がぶれる。

税社保タブ再設計は、税社保方針と用語整理が先に必要である。

保存方式は、限定公開の形態を決める前に最低限の方針を確定する必要がある。

シナリオ比較は、ダッシュボードの主要指標が決まった後に再設計する。

タイムバケットは、健康寿命期の資産活用指標と接続してから一般化する。

## 6. 作業順序

1. Phase 1: 現行版棚卸し
2. Phase 2: 情報設計と入力分類
3. Phase 3: データモデルと保存方針
4. Phase 4: 初回ウィザード
5. Phase 5: ダッシュボード
6. Phase 6: 税金・社会保険タブ
7. Phase 7: シナリオ比較
8. Phase 8: タイムバケット
9. Phase 9: 免責とヘルプ
10. Phase 10: QAと限定公開準備
11. Phase 11: 協力者向け安心説明
12. Phase 12: iDeCo一時金UIと退職所得控除重複調整
13. Phase 13: アプリ名称の統一
14. Phase 14: GitHub Pages 限定公開準備
15. Phase 15: 入力導線・カード折りたたみ UX再設計

## 7. 進捗管理表

| フェーズ | 状態 | 主担当領域 | 主な成果物 |
|---|---:|---|---|
| Phase 0 | 着手済み | 作業場所 | 一般向け版フォルダ、設計docs |
| Phase 1 | 完了 | 棚卸し | 棚卸し表、移行方針 |
| Phase 2 | 完了 | 情報設計 | 入力分類、用語表、匿名サンプル |
| Phase 3 | 完了 | データ | 保存方針、匿名サンプル |
| Phase 3.5 | 完了 | 保存分離 | 一般向け保存キー、個人用導線削除 |
| Phase 4 | 初期版完了 | 初回体験 | ウィザード |
| Phase 4.5 | 完了 | 入口整理 | 匿名サンプルデータ、一般向け初期シナリオ |
| Phase 5 | 初期版完了 | 結果表示 | ダッシュボード結論カード、税社保注意年 |
| Phase 6 | 初期版完了 | 税社保 | 税社保要約・注意年・詳細折りたたみ |
| Phase 7 | 初期版完了 | 比較 | 比較の結論、差分理由、詳細比較 |
| Phase 8 | 初期版完了 | 資産活用 | やりたいこと整理、テンプレート、金額未定導線 |
| Phase 9 | 初期版完了 | 信頼性 | 免責、ヘルプ、用語、保存先表示 |
| Phase 10 | 初期QA完了 | 品質 | QA記録、限定公開判定 |
| Phase 11 | 実装済み | 協力者向け説明 | データの扱い説明、協力者向け文面 |
| Phase 12 | 実装依頼作成済み | iDeCo一時金 | iDeCo受取方法UI、退職所得控除重複調整 |
| Phase 13 | 実装依頼作成済み | 名称統一 | ローカル版アプリ名変更 |
| Phase 14 | 実装済み | GitHub Pages | 限定公開用デプロイ |
| Phase 15 | 設計作成済み | 入力UX | 入力状況サマリー、カード折りたたみ、次アクション導線 |

## 8. 次に着手する作業

Phase 4 の前段として、保存混在と個人用導線を外す実装は完了した。

完了済み:

- LocalStorageキーを `retirement-life-planner-public-v1` に変更
- QNAP共有保存の自動読み込みと保存ボタンを削除
- `restore=chrome5173` と `実データを復旧` 導線を削除
- データ画面を一般向け文言に変更
- JSONファイル名を一般向けに変更
- `npm run test:public` 成功
- `npm run build:public` 成功

Phase 4 の初回ウィザード初期版は完了した。

実装済み:

- 初回設定ウィザードを追加
- 家族、資産、収入、支出、前提、やりたいこと、結果の7ステップを実装
- 年齢別生活費変更、インフレ率、資産別利回りを標準入力として配置
- 詳細な税社保設定は「今は標準前提で進む / 後で詳しく設定する」として選択可能にした
- 入力中の内容を元に結果ステップで試算プレビューを表示
- 保存すると表示中シナリオへ反映してダッシュボードへ戻る
- ヘッダーからいつでも初回設定を再表示できる
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` で初回設定の開閉と結果ステップを確認

Phase 4.5 の匿名サンプルデータ置き換えは完了した。

実装済み:

- 初期サンプルを「標準ケース」の匿名夫婦世帯に変更
- 開始年月を `2026-06` に変更
- 毎月生活費を32万円の一般向け初期値に変更
- 初期シナリオを「生活費を少し抑える」「年金受給を遅らせる」「楽しみ支出を増やす」「利回りを低めに見る」に変更
- 初期サンプルから CFD、米国株オプション、一般口座オプション中心の資産を外した
- オプション機能の回帰テストは、テスト内で必要な詳細口座を明示する形に変更
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` で匿名サンプル名が表示され、旧サンプル名が初期表示に出ないことを確認

Phase 5 のダッシュボード初期版は完了した。

実装済み:

- ダッシュボード先頭に「まず見る結論」を追加
- 資産寿命、目標残高との差額、生活費・税社保後の月次余力を先頭で表示
- 税社保の注意年と翌年負担を先頭で表示
- 楽しみ支出を健康寿命期の判断指標として先頭に表示
- 譲渡益税チャートの文言を一般向けに調整し、一般口座オプション前提の表現を弱めた
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` のデスクトップ幅とスマートフォン幅で先頭カード表示を確認

Phase 6 の税金・社会保険タブ初期版は完了した。

実装済み:

- 税金・社会保険タブの先頭に要約ビューを追加
- 税社保総額、最も負担が大きい所得年、現金で出ていく注意年を表示
- 直近5年の支払タイミングを所得税精算、住民税、国保、後期高齢、年金、介護、譲渡益税に分けて表示
- 概算・対応年度・自治体差・通知書補正の注意を先頭に表示
- 退職所得、所得控除、専門補正を「詳細入力・専門補正」として折りたたみ化
- 税社保タブ内の一部文言を一般向けに調整
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` のデスクトップ幅とスマートフォン幅で税社保要約を確認

Phase 7 のシナリオ比較初期版は完了した。

実装済み:

- 比較タブ先頭に「比較の結論」を追加
- 最も良さそうな案、注意して見る案、比較期間を先頭で表示
- 資産寿命、目標残高差、税社保増分、生活後余力差、NISA未実行差を横並びで表示
- 赤字は負担増・悪化、青緑は改善・負担減として説明を追加
- 既存の比較表は「複数シナリオ詳細比較表」として後段に残した
- 一般口座オプション寄りだった詳細見出しを、一般向けに「申告対象の運用利益」へ調整
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` のデスクトップ幅とスマートフォン幅で比較サマリーを確認

Phase 8 のタイムバケット一般化初期版は完了した。

実装済み:

- 「タイムバケット・プランナー」を「やりたいこと整理」へ一般向けに変更
- 金額未定・未支出化の件数を表示し、計算に入らないことを明示
- 支出化済み件数、楽しみ支出合計、支出化済み合計を先頭で表示
- 国内旅行、夫婦旅行、趣味・学び、家族イベント、住まい修繕、医療介護備えのテンプレートを追加
- テンプレート追加後も、金額と時期を決めて「支出へ」変換するまで計算に入らない導線にした
- カード内にメモを表示し、何のための仮置きか分かるようにした
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` のデスクトップ幅とスマートフォン幅で資産活用タブを確認

Phase 9 の免責、ヘルプ、用語、信頼性表示初期版は完了した。

実装済み:

- ヘッダー直下に概算、保存先、専門家確認の信頼性表示を追加
- マニュアル先頭に「信頼性と注意事項」を追加
- 2026年度前提、自治体差、制度改正、通知書との差を明示
- 保存先がご利用中の端末であり、端末間自動同期されないことを明示
- 重要判断前に年金事務所、自治体、税理士、社労士、FPなどで確認する案内を追加
- マニュアルに用語の読み替えを追加
- 資産寿命、生活費・税社保後の余力、目標残高、支出化、補正、保存用ファイルを説明
- `npm run test:public` 成功
- `npm run build:public` 成功
- `http://127.0.0.1:5176/` のデスクトップ幅とスマートフォン幅で信頼性表示を確認

Phase 10 の初期QAは完了した。

実施済み:

- `npm run test:public` 成功
- `npm run build:public` 成功
- 公開実行ソースの個人用文言検索を実施
- ダッシュボード、初回設定、資産活用、結果、比較、データ、マニュアルをブラウザ確認
- スマートフォン幅 390px で横スクロールなしを確認
- 注意帯の初回5秒、再表示後10秒の自動折りたたみを確認
- 開発中の保存済み旧サンプルを一般向け匿名サンプルへ移行する処理を追加
- QA記録を `docs/public-app/qa-limited-release-2026-05-19.md` に作成
- リリース確認リストを更新

追加整備済み:

- プライバシーポリシー案を `docs/public-app/privacy-policy-draft.md` に作成
- 問い合わせ・フィードバック導線を `docs/public-app/feedback-plan.md` に作成
- 限定公開運用計画を `docs/public-app/limited-release-plan.md` に作成
- アプリ内マニュアルへプライバシーとフィードバックの案内を追加

一般公開前に残っている判断事項:

- プライバシーポリシーを置く公開URL
- 問い合わせフォームまたは専用メールアドレス
- アクセス解析やログ収集を入れるかどうか

## Phase 11: 協力者向け安心説明（実装済み）

目的: 一般公開前の協力者試用で、入力データが作成者側へ送信されない設計を、非エンジニアにも分かる形で説明する。

工程表:

- `docs/public-app/collaborator-trust-workplan.md`

主な作業:

- 初回表示の「データの扱い」説明モーダルを追加
- ヘッダーから「データの扱い」を再表示できる導線を追加
- マニュアルへの図入り説明を追加
- データタブの削除・保存用ファイル注意導線を追加
- 協力者へ送る説明文とフィードバック依頼文を作成
- PC幅でのQA、テスト、ビルドを実施

完了条件:

- 協力者が「アプリ本体はWebから読み込むが、入力データは本人が使っている端末内に保存される」と理解できる。
- 作成者へ入力内容を送信する機能がないことを、画面内と配布文面の両方で説明できる。
- 保存用ファイルや画面の画像を自分で送る場合だけ内容が伝わることを明示できる。

実装・確認記録:

- `docs/public-app/collaborator-instructions.md`
- `docs/public-app/collaborator-message-short.md`
- `docs/public-app/collaborator-feedback-request.md`
- `docs/public-app/collaborator-feedback-questions.md`
- `docs/public-app/github-pages-limited-release.md`
- `docs/public-app/qa-collaborator-trust-2026-05-21.md`

## Phase 12: iDeCo一時金UIと退職所得控除重複調整の改善

追加日: 2026-06-05

目的: iDeCo一時金を「単発入金」として探させる分かりにくさを解消し、退職金を先に受け取った利用者でも税区分・重複調整・社会保険への影響を誤解しにくくする。

実装対象:

- `apps/retirement-life-planner-public`

参考実装:

- ローカル版 `apps/retirement-life-planner` には同趣旨の修正が反映済み。一般向け版では、公開向け文言・保存キー・匿名サンプル方針を保ったまま同等機能を実装する。

主な作業:

- iDeCoを原資資産として選んだ場合、収入イベントの「種別」を「iDeCo受取方法」に変える。
- 選択肢を `iDeCo年金受取（雑所得）` と `iDeCo一時金（一括受取・退職所得）` に絞る。
- 通常の汎用収入では `単発入金` を `一時金・単発入金` と表示する。
- iDeCo一時金入力に `加入開始日（任意）` と `加入終了日（任意）` を追加する。
- iDeCo一時金の退職所得控除重複調整で、日付が揃っている場合は期間入力ベース、日付が不足する場合は加入年数ベースにする。
- 年数ベースの場合も、加入年数を使っていることが分かる根拠文に変更する。
- iDeCo一時金は退職所得であり、国保・後期高齢者医療の所得割には含めない旨を補助文に出す。
- 住民税は通常所得の翌年負担と混同させず、退職所得の分離課税として扱う説明にする。
- シミュレーションテストに、iDeCo一時金の加入開始日/終了日がある場合に期間入力ベースへ切り替わるケースを追加する。

完了条件:

- `npm --prefix apps/retirement-life-planner-public run test -- src/lib/simulation.test.ts` が成功する。
- `npm --prefix apps/retirement-life-planner-public run build` が成功する。
- `http://127.0.0.1:5176/` でiDeCo一時金フォームに加入開始日/終了日が表示される。
- iDeCo原資選択時に「iDeCo受取方法」と2つの専用選択肢が表示される。
- 退職所得控除の重複調整表で、年数ベース/期間入力ベースの根拠文が誤解なく表示される。

## Phase 13: アプリ名称の統一

追加日: 2026-06-05

目的: ローカル版も一般向け版と同じ「人生資産シミュレーション」という名称へ統一し、旧名称「退職後生活シミュレーション」から移行する。

実装対象:

- `apps/retirement-life-planner`

主な作業:

- ブラウザタイトルを「人生資産シミュレーション」に変更する。
- 画面上の主タイトルを「人生資産シミュレーション」に変更する。
- 必要に応じて `本人専用MVP` 等の補足は残してよいが、主タイトルより目立たせない。
- READMEやショートカット説明に旧名称がある場合は、内部向けメモとして必要な箇所以外を新名称へ更新する。

完了条件:

- `http://127.0.0.1:5175/` のタブタイトルと画面主タイトルが「人生資産シミュレーション」になっている。
- `npm --prefix apps/retirement-life-planner run build` が成功する。

## Phase 14: GitHub Pages 限定公開準備

目的: 協力者がURLを開くだけで一般向けアプリを試せるようにする。

実施内容:

- GitHub Pagesの公開対象を `apps/retirement-life-planner-public` に切り替える
- GitHub Actionsで `npm run test:public` と `GITHUB_PAGES=true npm run build:public` を実行する
- 公開成果物を `apps/retirement-life-planner-public/dist` にする
- 協力者へ送る短い案内文を作成する

成果物:

- `.github/workflows/deploy-pages.yml`
- `docs/public-app/github-pages-limited-release.md`
- `docs/public-app/collaborator-message-short.md`

## Phase 15: 入力導線・カード折りたたみ UX再設計

追加日: 2026-06-05

目的: 一般向け版の通常入力画面で、カードが多く並びすぎる問題を解消し、ユーザーが次に必要な入力へ自然に進めるようにする。

設計書:

- `docs/public-app/ux-guided-input-card-collapse-design.md`

実装担当向けプロンプト:

- `docs/handoff/implementation-prompt-public-guided-input-ux-2026-06-05.md`

実装対象:

- `apps/retirement-life-planner-public`

主な作業:

- `安全性シミュレーション` のサブタブ上部に入力状況サマリーを追加する。
- 入力カードを `必須`、`推奨`、`詳細`、`専門補正` に分類する。
- カードの表示状態を `常時表示`、`要約表示`、`折りたたみ`、`非表示` に分ける。
- 未完了の必須カード、結果から指定されたカード、確認推奨カードだけを初期展開する。
- 完了済みカードは主要値と状態だけを要約表示する。
- 計算に使っていないカードは全表示せず、`計算対象外` の要約行にする。
- 入力完了後、次に必要なカードを枠色、`次に入力` ラベル、タブバッジで促す。
- 入力後に `次へ` ボタンで次の未入力カードへ移動できるようにする。
- 初期資産、生活費、収入、税・社会保険、結果、ダッシュボードの詳細カードを折りたたみ中心に整理する。
- 結果画面やダッシュボードの注意表示から、該当する入力カードや根拠カードへ戻れるリンクを追加する。

導入順:

1. Phase 15-A: カード状態と入力状況サマリー
2. Phase 15-B: 入力カードの折りたたみ
3. Phase 15-C: 結果・ダッシュボードの原因調査導線
4. Phase 15-D: QAと表示検証
5. Phase 15-E: 入力ナビゲーション強化
6. Phase 15-F: 初期資産タブの表示優先度と文言修正
7. Phase 15-G: 確認推奨カードの確認済み機能

完了条件:

- 通常入力画面の先頭で、必須入力の完了状況と次に入力すべき項目が分かる。
- 1つのカード入力後に、次に必要なカードが視覚的に分かる。
- 各入力タブの初期表示で、すべてのカードが全体表示されていない。
- 詳細入力、専門補正、原因調査、コピー操作、年度別根拠が初期状態で閉じている。
- 年金受給プランナー反映中の外部公的年金イベントなど、計算対象外カードが全表示されない。
- 初期資産、生活費、収入の初期表示が標準入力中心になり、詳細入力は必要時に開ける。
- 結果画面の初期表示が結論中心になり、原因調査用の表やチャートは折りたたまれている。
- ダッシュボードまたは結果の注意表示から、該当入力・根拠へ移動できる。
- `npm --prefix apps/retirement-life-planner-public run test -- src/lib/simulation.test.ts` が成功する。
- `npm --prefix apps/retirement-life-planner-public run build` が成功する。
- `http://127.0.0.1:5176/` のPC幅とスマートフォン幅で、入力しやすさと表示崩れが確認されている。

### Phase 15-E: 入力ナビゲーション強化

追加日: 2026-06-08

目的: 入力状況サマリーと `次` マークの意味を初回ユーザーにも分かるようにし、サマリーから対象入力欄まで確実に移動できるようにする。

実装担当向けプロンプト:

- `docs/handoff/implementation-prompt-public-input-navigation-strengthening-2026-06-08.md`

主な作業:

- `6/6 完了` のような表記を、`6項目中6項目完了` または `必須入力は完了` に変更する。
- 必須入力の内訳を `内訳を見る` で確認できるようにする。
- 必須が完了していて推奨項目だけが残る場合、`次に入力` ではなく `次に確認するとよい` と表示する。
- 入力状況サマリーの `次に入力` / `次に確認するとよい` 枠全体をクリック可能にする。
- クリック時に、対象タブへ切り替え、対象カードを展開し、最初に必要な入力欄へスクロール・フォーカスする。
- 対象タブの `次` バッジに、次対象が分かる説明を持たせる。
- `ダッシュボード` と `結果` からも入力状況サマリーまたは次対象へ移動できる導線を出す。

完了条件:

- 初回ユーザーが `6/6` の意味を理解できる表示になっている。
- `iDeCo受取` が次対象の場合、サマリーの枠をクリックすると `収入` タブのiDeCo受取欄へ移動する。
- 対象カードだけでなく、可能なら `iDeCo受取方法` の入力欄へフォーカスする。
- `収入` タブの `次` バッジが、次対象が収入タブ内にあることを示す。
- 入力状況サマリーが、特定タブを偶然開いた時だけ見つかる状態ではない。

### Phase 15-F: 初期資産タブの表示優先度と文言修正

追加日: 2026-06-08

目的: 初期資産タブで、一般口座サブ口座が主役に見える状態を避け、NISA・iDeCo・特定口座などの評価額・評価損益入力を次に確認すべき対象として強調する。

実装対象:

- `apps/retirement-life-planner-public`
- `apps/retirement-life-planner`

実装担当向けプロンプト:

- `docs/handoff/implementation-prompt-all-versions-asset-tab-priority-2026-06-08.md`

主な作業:

- `証券・iDeCoの評価額と評価損益` を `NISA・iDeCo・特定口座などの評価額と評価損益` に変更する。
- 同カードを、初期資産タブで次に確認すべき対象として色付き強調できるようにする。
- 一般口座（申告対象運用）のサブ口座は、初期表示では畳む。
- 一般口座サブ口座は、ユーザーが詳細管理を開いた時、または結果・税社保から原因調査で指定された時だけ展開する。
- ローカル版と公開版の両方で同じ文言・表示優先度にする。

完了条件:

- 初期資産タブで、一般口座サブ口座が初期表示の主役になっていない。
- `NISA・iDeCo・特定口座などの評価額と評価損益` が確認推奨または次対象として強調表示される。
- 公開版とローカル版の両方で文言が統一されている。
- 両アプリのテストとビルドが成功する。

### Phase 15-G: 確認推奨カードの確認済み機能

追加日: 2026-06-08

目的: `退職所得控除の重複調整` など、入力不足ではなく確認が必要なカードについて、ユーザーが内容確認後に `確認済み` として扱えるようにする。確認済み後は入力状況サマリーの黄色い次対象から外し、関連入力が変わった時だけ再確認を求める。

設計書:

- `docs/public-app/review-acknowledgement-design-2026-06-08.md`

実装担当向けプロンプト:

- `docs/handoff/implementation-prompt-all-versions-review-acknowledgement-2026-06-08.md`

実装対象:

- `apps/retirement-life-planner-public`
- `apps/retirement-life-planner`

主な作業:

- 入力カード状態に `確認済み` を追加する。
- シナリオ単位で確認済み状態を保存する。
- 確認対象のfingerprintを作り、入力値が変わった場合は再び `確認推奨` に戻す。
- `退職所得控除の重複調整` カード内に `この内容を確認済みにする` ボタンを追加する。
- 入力状況サマリーでは、確認済みカードを黄色い次対象から外す。
- 詳細一覧には `確認済み` として残し、必要なら再確認できるようにする。

完了条件:

- 退職所得控除の重複調整を確認済みにすると、入力状況サマリーの黄色い表示から外れる。
- カードを開くだけでは確認済みにならない。
- 金額、年月、勤続/加入期間などを変更すると、再び確認推奨になる。
- ローカル版と公開版の両方で同じ挙動になる。
- 両アプリのテストとビルドが成功する。

## Phase 16: ローカル版・GitHub公開版の仕様差分反映

追加日: 2026-06-08

目的: ローカル版とGitHub公開版の差分を監査し、公開版へ未反映の承認済みUX改善を実装する。ただし、本人専用・QNAP共有・実データ復旧など、公開版に戻してはいけない個人環境機能は除外する。

設計書:

- `docs/public-app/local-vs-public-gap-audit-2026-06-08.md`

実装担当向けプロンプト:

- `docs/handoff/implementation-prompt-public-gap-porting-2026-06-08.md`

実装対象:

- 主対象: `apps/retirement-life-planner-public`
- 比較対象: `apps/retirement-life-planner`

主な作業:

- `origin/main` と作業ツリーの差分を確認し、GitHub公開版に未反映の変更を特定する。
- 初期資産タブの文言・表示優先度・一般口座サブ口座折りたたみを公開版へ反映する。
- 入力状況サマリーの文言、次対象クリック、対象タブ・対象カード・対象入力欄への移動を強化する。
- 公開版のカード初期表示を、必須・推奨・詳細・専門補正の分類に合わせて整理する。
- 公開版にQNAP共有保存、実データ復旧、本人専用文言が戻っていないことを検索確認する。
- 公開版固有の初回設定ウィザード、データの扱い説明、GitHub Pages向け表示、公開版保存キーは残す。

完了条件:

- 公開版に、承認済みの入力UX改善が反映されている。
- 公開版に、QNAP共有保存や実データ復旧などの個人環境機能が混入していない。
- 初期資産タブで `NISA・iDeCo・特定口座などの評価額と評価損益` が確認推奨として見える。
- 一般口座サブ口座が初期表示で畳まれている。
- 入力状況サマリーの次対象から対象入力欄へ移動できる。
- `npm --prefix apps/retirement-life-planner-public run test -- src/lib/simulation.test.ts` が成功する。
- `npm --prefix apps/retirement-life-planner-public run build` が成功する。
- GitHub Pagesへ反映する場合は、push後に `https://jimusaku-lab.github.io/retirement-simulator/` で確認できる。
